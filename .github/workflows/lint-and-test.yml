name: 'Lint and Unit Tests'

on:
  workflow_call:


jobs:
  toolkit-package:
    uses: ./.github/workflows/_build-toolkit.yml

  cpp-linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 2
      - uses: cpp-linter/cpp-linter-action@main
        id: linter
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          style: file
          tidy-checks: '-*'
          files-changed-only: true
          lines-changed-only: diff
          format-review: false
          step-summary: true
          version: 20

      - name: Show formatting diff
        if: steps.linter.outputs.checks-failed != 0
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "::group::clang-format diff details"
          # Only check files changed in this PR/push, matching cpp-linter's behavior
          if [ -n "${{ github.event.pull_request.number }}" ]; then
            FILES=$(gh pr diff ${{ github.event.pull_request.number }} --name-only | grep -E '\.(c|cc|cpp|h|hpp)$' || true)
          else
            FILES=$(git diff --name-only HEAD~1 -- '*.c' '*.cc' '*.cpp' '*.h' '*.hpp' 2>/dev/null || true)
          fi

          if [ -z "$FILES" ]; then
            echo "No changed C/C++ files found."
          else
            echo "$FILES" | while read -r file; do
              if [ -f "$file" ]; then
                DIFF=$(diff -u "$file" <(/usr/bin/clang-format-20 -style=file "$file") || true)
                if [ -n "$DIFF" ]; then
                  echo "::warning file=$file::File does not conform to clang-format style"
                  echo "$DIFF"
                  echo ""
                fi
              fi
            done
          fi
          echo "::endgroup::"

      - name: Fail fast?!
        if: steps.linter.outputs.checks-failed != 0
        run: |
          echo "::error::cpp-linter found ${{ steps.linter.outputs.checks-failed }} formatting issue(s). See the diff output above or the job summary for details."
          exit 1

  py-linter:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Add matchers for better error display
      run: |
        echo "::add-matcher::.github/workflows/matchers/actionlint.json"
        echo "::add-matcher::.github/workflows/matchers/mypy.json"

    - name: Run pre-commit checks on all files
      uses: pre-commit/action@v3.0.1
      env:
        SHELLCHECK_OPTS: "--exclude=SC2046,SC2006,SC2086"
      with:
        extra_args: --all-files --hook-stage manual

  cpp_gtest:
    runs-on: ubuntu-latest
    env:
      BUILD_TYPE: Debug
    steps:
    - uses: actions/checkout@v4

    - name: Configure CMake
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} -DBUILD_UCM_SPARSE=OFF -DBUILD_UNIT_TESTS=ON -DRUNTIME_ENVIRONMENT=simu -DBUILD_UCM_ASU=ON

    - name: Build
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}} -j

    - name: Test
      working-directory: ${{github.workspace}}/build
      run: ctest -C ${{env.BUILD_TYPE}} --output-on-failure
